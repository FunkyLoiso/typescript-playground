export class Project {
    constructor(id: string, name: string, color: string) {
        this.id = id
        this.name = name
        this.color = color
    }
    id: string;
    name: string;
    color: string;
}
